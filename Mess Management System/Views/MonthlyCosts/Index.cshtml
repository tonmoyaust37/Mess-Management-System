@model IEnumerable<Mess_Management_System.Models.MonthlyCost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Month_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.avg_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.individual_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.manager.managerId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Month_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.total_cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.avg_cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.individual_cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.manager.managerId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.MonthlyCostId }) |
                @Html.ActionLink("Details", "Details", new { id = item.MonthlyCostId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.MonthlyCostId })
            </td>
        </tr>
    }

</table>
<h2>Total cost: @ViewData["totalcost"]</h2>
<br />
<h2>Total meal: @ViewData["totalmeal"]</h2>
<br />
<h2>avg cost: @ViewData["res"]</h2>